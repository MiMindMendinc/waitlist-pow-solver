# Waitlist POW Solver — environment variables
# Copy this file to .env and fill in your values.

# The base URL of the Cloudflare Worker waitlist endpoint
WAITLIST_URL=https://waitlist.example.com

# The email address to register on the waitlist
WAITLIST_EMAIL=user@example.com

# Optional: maximum milliseconds to wait for a response (default: 5000)
WAITLIST_TIMEOUT=5000

# Optional: enable verbose logging (true | false, default: false)
WAITLIST_VERBOSE=false
